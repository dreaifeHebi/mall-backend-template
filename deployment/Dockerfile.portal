FROM eclipse-temurin:8-jre-alpine

WORKDIR /app
RUN mkdir -p /app/logs /data/uploads

COPY mall-portal.jar /app/app.jar

EXPOSE 8085
ENTRYPOINT ["sh", "-c", "java ${JAVA_OPTS:-} -jar -Dspring.profiles.active=${SPRING_PROFILES_ACTIVE:-prod} /app/app.jar"]
