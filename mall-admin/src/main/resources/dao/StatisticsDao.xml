<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.dao.StatisticsDao">

    <select id="getOrderStatistics" resultType="com.macro.mall.dto.OrderStatisticsDto">
        SELECT 
            DATE(create_time) as date,
            COUNT(*) as orderCount,
            COALESCE(SUM(total_amount * 100), 0) as orderAmount
        FROM oms_order 
        WHERE delete_status = 0
            AND DATE(create_time) BETWEEN #{startDate} AND #{endDate}
        GROUP BY DATE(create_time)
        ORDER BY date ASC
    </select>

</mapper>
